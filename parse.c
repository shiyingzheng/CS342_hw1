#include <parse.h>
#include <stdio.h>
#include <regex.h>

int main( int argc, char *argv[] )
{
    url = argv[0];
    protocol = regcomp(&url, "", 0);
    website = regcomp();
    resource = regcomp();
}
struct hostent* parse(char* url, char* path, char* path) {
}
